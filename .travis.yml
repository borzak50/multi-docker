sudo: required
services:
  - docker

before_install:
  - docker build -t borzak50/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true borzak50/react-test npm test

after_success:
  - docker build -t borzak50/multi-client ./client
  - docker build -t borzak50/multi-nginx ./nginx
  - docker build -t borzak50/multi-server ./server
  - docker build -t borzak50/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push borzak50/multi-client
  - docker push borzak50/multi-nginx
  - docker push borzak50/multi-server
  - docker push borzak50/multi-worker
